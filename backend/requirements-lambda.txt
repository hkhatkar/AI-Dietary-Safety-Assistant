# Same as requirements.txt but without uvicorn - Lambda uses Mangum to adapt the ASGI
# app directly, uvicorn is only needed for local dev, so it's left out of the deployed
# package to keep it smaller.
fastapi==0.115.6
pydantic==2.10.4
python-dotenv==1.0.1
mangum==0.19.0
anthropic==0.42.0
boto3==1.35.99
